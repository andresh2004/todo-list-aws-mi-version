pipeline {
    agent any

    stages {
        stage('Get Code') {
            steps {
                echo '📥 Descargando el código desde GitHub...'
                // Jenkins ya hace checkout si el pipeline está bien conectado al repo
            }
        }

        stage('Static Test') {
            steps {
                echo '🔍 Ejecutando análisis estático con flake8...'
                sh 'pip install --user flake8'
                sh 'export PATH=$PATH:$HOME/.local/bin && flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics'
            }
        }
    }
}
